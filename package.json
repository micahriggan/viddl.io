{
  "name": "viddl",
  "version": "1.0.0",
  "description": "download youtube videos",
  "main": "index.js",
  "scripts": {
    "start": "./node_modules/.bin/lerna run start --parallel",
    "backend:prod": "./node_modules/.bin/pm2 start ./packages/viddl-be/ts_build/index.js --env production", 
    "bootstrap": "./node_modules/.bin/lerna bootstrap",
    "compile": "./node_modules/.bin/lerna run compile",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Micah Riggan",
  "license": "MIT",
  "devDependencies": {
    "lerna": "^3.5.1",
    "pm2": "^3.2.2"
  }
}
